server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    autoindex off;

    location = / { return 302 /paygen/; }
    location /paygen/ { try_files $uri $uri/ =404; }
    location / { return 404; }
    error_page 404 /paygen/404.html;
}
